<% provide(:title, 'En attente de licence NQA') %>
<div id="themeQuidditchConnecte">
<h1>En attente de licence NQA</h1>

<%= will_paginate %>

<ul class="users"> 
	<% @users.each do |user| %> 
		<% if user.want_do_sport == true %> 
		<li> 
			<%= gravatar_for user, size: 52 %> 
			<%= link_to user.name, user %> 
			<% if current_user.admin? && !current_user?(user) %>
			| <%= link_to "supprimer", user, method: :delete, data: { confirm: "Etes-vous sûr?" } %> 
			<% end %> 
		</li> 
		<% end %> 
	<% end %> 
</ul>
<%= will_paginate %>
</div>
